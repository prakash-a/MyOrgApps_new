trigger contact on Account (after delete, after insert, after update) {
	 // do something;
	 
	 //Contact_Handler contactHandler = new Contact_Handler();
	 
	 if(trigger.isInsert)
	 {
	 	
	 }
	 else if(trigger.isUpdate)
	 {
	 	
	 }
	 else if(trigger.isDelete)
	 {
	 	
	 }
}
/*
public with sharing class Contact_Handler
{
	public Contact_Handler()
	{
		// do something
	}
}
*/